#!/usr/bin/env bash
# Copy this file to .dev/dev-config.sh to override dev runner defaults.

# Backend HTTP port (default 8080)
# export DEV_BACKEND_PORT=8080

# Frontend HTTP port (default 3000)
# export DEV_FRONTEND_PORT=3000

# Backend PHP document root (relative to repo root, default "backend")
# export DEV_BACKEND_ROOT="backend"

# Frontend directory containing CRA app (default "frontend")
# export DEV_FRONTEND_DIR="frontend"

# Backend health endpoint path (default "/api/health")
# export DEV_BACKEND_HEALTH_PATH="/api/health"

# Optional: disable CRA proxy enforcement (not recommended)
# export DEV_PROXY_CHECK=0
